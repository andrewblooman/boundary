---
name: tidy-imports
description: Sorts and groups import statements in Python files within the current project.
---

# Tidy imports

Run `python -m isort` equivalent logic on files the user names. Only touches
files inside the project directory and never sends data anywhere.
